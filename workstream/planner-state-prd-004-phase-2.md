# Planner State: prd-004-phase-2

## Run Info

- Task source: `workstream/tasks-prd-004-phase-2-plan.md`
- Integration branch: `integration/prd-004-phase-2-banks-kinds-sessions-recall`
- Repository: `llipe/memo-cli`
- Started: 2026-09-21
- Last updated: 2026-09-22 (post S2-11 merge — **all 11 stories complete**)

## Story Status

| Sequence | Story ID | Issue # | Status       | PR                                                                                                                                                                                     | Branch                                                                                    |
| -------- | -------- | ------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1        | S2-01    | #53     | ✅ Merged    | [#92](https://github.com/llipe/memo-cli/pull/92)                                                                                                                                       | `story/s2-01-config-v2-schema-v2` (deleted)                                               |
| 2        | S2-02    | #81     | ✅ Merged    | [#93](https://github.com/llipe/memo-cli/pull/93)                                                                                                                                       | `story/S2-02-qdrant-repository-extensions` (deleted)                                      |
| 3        | S2-03    | #82     | ✅ Merged    | [#94](https://github.com/llipe/memo-cli/pull/94)                                                                                                                                       | `story/S2-03-bank-filters-dedupe-v2` (deleted)                                            |
| 4        | S2-04    | #83     | ✅ Merged    | [#95](https://github.com/llipe/memo-cli/pull/95)                                                                                                                                       | `story/S2-04-memo-write-v2` (deleted)                                                     |
| 5        | S2-05    | #84     | ✅ Merged    | [#96](https://github.com/llipe/memo-cli/pull/96)                                                                                                                                       | `story/S2-05-read-flags-bank-staleness` (deleted)                                         |
| 6        | S2-06    | #85     | ✅ Merged    | [#97](https://github.com/llipe/memo-cli/pull/97)                                                                                                                                       | `story/S2-06-memo-timeline` (deleted)                                                     |
| 7        | S2-07    | #86     | ✅ Merged    | [#100](https://github.com/llipe/memo-cli/pull/100)                                                                                                                                     | `story/S2-07-memo-recall` (deleted)                                                       |
| 8        | S2-08    | #87     | ✅ Merged    | [#101](https://github.com/llipe/memo-cli/pull/101)                                                                                                                                     | `story/S2-08-bank-inspect-facet` (deleted)                                                |
| 9        | S2-09    | #88     | ✅ Merged    | [#102](https://github.com/llipe/memo-cli/pull/102)                                                                                                                                     | `story/S2-09-memo-migrate-to-v2` (deleted)                                                |
| 10       | S2-10    | #89     | ✅ Delivered | [dev-tasks#240](https://github.com/llipe/dev-tasks/pull/240) (pending human review, separate repo) + [#103](https://github.com/llipe/memo-cli/pull/103) (merged, skill-sync follow-up) | `issue/89-memo-cli-usage-v13` (dev-tasks), `issue/89-sync-memo-cli-usage-skill` (deleted) |
| 11       | S2-11    | #90     | ✅ Merged    | [#104](https://github.com/llipe/memo-cli/pull/104)                                                                                                                                     | `story/S2-11-phase-2-exit-gate` (deleted)                                                 |

## Current Position

- **All 11 stories merged. Next: PRD-level rollup `verifier` audit, then Phase 5 (consolidated PR from integration branch to `main`).**
- Last merged PR: [#104](https://github.com/llipe/memo-cli/pull/104) (S2-11, squash-merged, branch deleted). **Open, human-review-pending PR:** [llipe/dev-tasks#240](https://github.com/llipe/dev-tasks/pull/240) — not merged by planner (targets another repo's `main`, same rule as never merging into memo-cli's own `main`).
- Integration branch HEAD: `0e330a7`

## Decisions Log

- 2026-09-21: Prep work (PRD v1.12, spec v1.3, 11 stories, 11 issues, task list, 11 Design Mode test plans) merged to `main` via PR #91 before this run started, so the integration branch carries every `/workstream` artifact the story handoffs reference by path.
- 2026-09-21: Execution order is strictly the dependency chain S2-01→02→03→04/05/06→07→08/09→10→11; planner's sequential invariant means even independent pairs run one at a time.
- 2026-09-21: S2-10 is a cross-repo (RF-63) consumer story targeting `llipe/dev-tasks` — memo-cli side has no code changes for it.
- 2026-09-21: S2-01 merged. `qa-engineer` PASS. `verifier`: Fidelity High, one Minor/Intended drift (D-1, AC2 wording, functionally inert). Queued.
- 2026-09-21: `gh pr merge`/`git pull`/`pnpm test` have all hit transient classifier or network hiccups this run (at least five times total) — always resolved by a straightforward retry, never a real blocker.
- 2026-09-21: Learned that `developer` subagents share this session's working directory (no worktree isolation) — a subagent's branch checkout persists after it finishes and can bleed uncommitted changes across branches. Now checking `git status` before every branch switch. `qa-engineer`/`verifier` use isolated worktrees on their own.
- 2026-09-21: S2-02 merged. `qa-engineer` PASS. `verifier`: Fidelity High, D-1 (Minor/Intended, inert, queued) + **D-2 (Major/Intended)**: `fetchStalenessCorpus` built a private base-filter copy instead of composing S2-03's `buildBaseFilter`. **D-2 actioned immediately**: added task 3.7 + verification steps to task 3.0 (still open at the time), posted to issue #82.
- 2026-09-21: S2-03 merged. `qa-engineer` PASS (confirmed structurally the D-2 parity tests would fail pre-fix). `verifier`: Fidelity High, **zero drift** — D-2 confirmed genuinely closed.
- 2026-09-21: First S2-04 `developer` attempt **failed mid-run from a transient network error**, leaving ~1,130 lines uncommitted on the integration branch. Recovered via `git stash push -u`, relaunched a fresh run that critically re-evaluated (not blindly trusted) the salvaged work. No work lost.
- 2026-09-22: S2-04 merged (PR #95). `qa-engineer` PASS globally, but caught two things the developer's self-report missed: `write.ts` file-level coverage below its own DoD threshold (52% functions), and the developer's "flipped global gate FAIL→PASS" claim didn't hold under independent measurement. Both Medium, queued. `verifier`: Fidelity High, all 11 ACs Pass. One Minor/Undetermined D-1 (harmless dead-code condition), queued. Report: `workstream/fidelity-report-issue-83.md`.
- 2026-09-22: S2-05 merged (PR #96) — carried PRD's AC-2.3/AC-2.4 safety guarantees. `qa-engineer` **injected a deliberate regression** into `buildBaseFilter` to prove the AC-2.3 gate genuinely catches regressions (it did: 5/5 tests failed), then reverted. PASS. `verifier`: Fidelity High, **zero drift**, transparently caught and discarded its own worktree contamination mid-audit. One incidental tested fix (`resolveBank` crash on missing `bank` key). Live `memo_eval` manual check not performed (no credentials) — queued.
- 2026-09-22: S2-06 merged (PR #97). `qa-engineer`: **`coverage_gate: FAIL`** — `timeline.ts` branch coverage 63.82% (below 75% threshold); two test-plan-committed scenarios (grouped/empty human-mode rendering) have zero test coverage. `verifier`: Fidelity Medium, highest drift **Major/Unintended (D-1)**: `parseSince()`'s date-only validation checks digit-shape but not calendar validity — `--since 2026-13-40` silently passes instead of failing `VALIDATION_FAILED`. Same bug inherited in the already-merged `list-filters.ts` (`normalizeIsoBoundary`, from S2-03). Per this run's merge-gate contract, FAIL/drift results don't block the merge (only an omitted result would) — merged as-is. **D-1 actioned immediately as a new follow-up issue** (not reopening the closed S2-03/S2-06 tasks, per drift-reconciliation convention for closed scope): a correct reference implementation already exists in `read-flags.ts` (`normalizeAsOf`/`isValidCalendarDate`), so the fix is to extract it into a shared module and reuse it in both broken call sites. Issue creation delegated to `github-ops`; fix delegated to `developer` as its own small PR before S2-07 resumes. The coverage gaps (grouped/empty rendering tests) are folded into that same fix issue.
- **Drift reconciliation queue (batched, non-blocking, for the S2-11 exit gate or sooner):** S2-01 D-1 (AC2 wording), S2-02 D-1 (`scrollAll` zero-page handling), S2-04 D-1 (dedupe fallback condition, Undetermined), S2-04 write.ts-coverage gap, `TESTING.md` narrative correction, S2-05 live `memo_eval` manual check outstanding, S2-08 traceability-matrix-not-populated pattern (all 8 stories so far), S2-09 D-1 (`stability` silently omitted under a non-default config), S2-09 D-2/hardening recommendation (`migrate --to-v2`'s real-run path shares every other command's `DEFAULT_COLLECTION_NAME = 'decisions'` fallback — both `qa-engineer` and `verifier` independently flagged this as worth a follow-up given it's the one command that mutates data; consider requiring `MEMO_COLLECTION` to be explicit for `migrate --to-v2`, or an extra confirmation gate, before the eventual production `decisions` migration).
- 2026-09-22: Issue #98 fix merged (PR #99). First `developer` attempt at this fix **also failed mid-run from a transient network error** (same class as S2-04's) while opening the PR — but it had already committed a complete, correct fix before failing, so nothing was lost. Recovery run critically re-verified the salvaged commit against issue #98's 6 acceptance criteria line-by-line rather than trusting it, found it sound, and finished the PR. `qa-engineer`: `coverage_gate: PASS` — confirmed the calendar-validity check is a genuine round-trip (not a naive isNaN), confirmed `read-flags.ts`'s refactor is behavior-neutral (subtractive-only diff), confirmed the new output tests assert real rendered content. `verifier` Audit Mode: Fidelity High, **zero drift**, explicitly re-checked and confirmed D-1 is genuinely closed. This closes out the one non-batched drift item; the rest remain queued below.
- 2026-09-22: S2-07 merged (PR #100) — the phase's headline feature (PRD goal 3, "one call to restore context"). Found and fixed a genuine production-affecting infra bug during live manual validation: `pending_contradiction` (needed by the CONFLICTS section) had no Qdrant payload index, causing a 400 on a strict-mode cluster — added as the 13th v2 index, additive-only, verified not to disturb the other 22. `rankCandidates()` was extracted from `search.ts` with a confirmed zero-diff on `search.ts`'s own tests (genuine behavior-preserving refactor). `qa-engineer`: `coverage_gate: PASS` — verified the AC8 zero-writes guarantee via real mock-call assertions and the SELF-never-trimmed adversarial test (SELF alone exceeding budget; every other section, including ones that started empty, correctly dropped into `truncated`). `verifier` Audit Mode: Fidelity High, **zero drift** on all 10 ACs, independently re-verified structurally (grepped for absent setPayload/fs imports, diff-inspected the index addition, re-ran the full suite on a clean worktree). Report: `workstream/fidelity-report-issue-86.md`. Also cleaned up 19 stray macOS/iCloud "keep both" duplicate files (`* 2.ts`/`* 2.md`) left behind by cloud-sync conflicts across several subagents writing to this shared working directory over the session — all confirmed harmless/stale before deletion.
- 2026-09-22: **Incident — accidental direct push to `main`.** After merging PR #99, a routine `git pull origin integration/prd-004-phase-2-banks-kinds-sessions-recall --ff-only` was run without first re-confirming the checked-out branch name; local HEAD had silently drifted to `main` at some point (most likely a `developer` subagent — which shares this session's working directory — running its own internal `git checkout` earlier, e.g. during S2-01's manual CLI validation against a built binary). `git pull origin <branch>` merges the named _remote_ branch into whatever is _currently checked out_, regardless of name match, so this fast-forwarded local `main` to the integration branch's tip; the next commit+push (the routine checkpoint update) landed directly on `origin/main` (`b34f7e2` → `c16f4be`), fully bypassing the consolidated-PR review/approval step. Caught immediately via the push output's `main -> main` line, reported to the user in full before any further action. The content itself was not corrupted (every commit had already passed `qa-engineer` + `verifier` gates before reaching the integration branch) — the violation was procedural: skipping the user's review checkpoint. **Resolved**: user confirmed revert; local `main` reset to `b34f7e2` (`git reset --hard`), but the push-to-main was blocked by this repo's own `git-guard.sh` hook even for the corrective action — reported verbatim rather than routed around, and the user ran the force-push themselves (`git push origin main --force-with-lease`, confirmed `c16f4be...b34f7e2 main -> main (forced update)`). The one real checkpoint commit made during the incident (`c16f4be`, the routine "post issue #98 merge" log update) was recovered via `git cherry-pick` onto the correct integration branch rather than lost. **Process fix going forward:** every `git checkout`/`git pull`/`git push` from here on is preceded by an explicit `git branch --show-current` check, not just `git status`, since `git status --short` alone does not print the branch name.
- 2026-09-22: S2-08 merged (PR #101). `qa-engineer`: `coverage_gate: PASS` — confirmed all three §18.14 resolutions (item 11 `writeConfig` gating, item 12 kind×state count matrix, item 13 banks-facet independence from narrowing flags) via real mock-call assertions; confirmed `write.ts` is genuinely zero-diff despite `bank init` delegating to it. `verifier` Audit Mode: Fidelity High, all 6 ACs Pass. One Minor/Undetermined drift (pre-existing `technical-guidelines.md` project-tree staleness missing several recent files, predates this story) — folded into batch docs cleanup. `qa-engineer` also flagged that `workstream/traceability-matrix-issue-87.md` was never populated post-implementation (still reads "Pending" throughout) — this looks like a **pattern across the whole phase's stories**, not story-specific; queued for a batch fix rather than treated as one-off drift. Report: `workstream/fidelity-report-issue-87.md`.
- **Pattern noted across S2-01–S2-08:** none of the `workstream/traceability-matrix-issue-*.md` files have been updated post-implementation by any `developer` run — they remain in their pre-implementation Design Mode state. This is consistent across every merged story so far, so it's a process gap (the closeout payload schema has no field prompting this write-back) rather than any individual story's oversight. Adding to the drift queue as one batched item rather than eight separate ones.
- 2026-09-22: S2-09 merged (PR #102) — the phase's one story with a real, live data-migration lifecycle; gated with the strictest scrutiny of the phase. Live evidence: dry-run and real-run against `MEMO_COLLECTION=memo_eval` both reported `{scanned:44, migrated:44, by_rule:{"1":4,"2":40}}`, second run `{scanned:0}` (idempotent), post-migration `eval:relevance` unchanged at 96.4%. `decisions` was never targeted by any command in this story. `qa-engineer`: `coverage_gate: PASS` — verified at the assertion level that dry-run writes zero, no `delete*` call exists anywhere reachable from the command, and idempotency holds by construction (double-guarded: Qdrant's own filter excludes migrated points, and the planner independently re-skips them). `verifier` Audit Mode: Fidelity High, all 6 ACs Pass, two Minor items — D1 (an unreachable-under-shipped-defaults config edge case where `stability` could be silently omitted) and D2 (the task list's "request human confirmation before any real apply" step was satisfied via this story's pre-approved `memo_eval`-only autonomous scope rather than a live pause — explicitly authorized in my delegation, and `verifier` was asked to name this plainly rather than soften it, which it did). Report: `workstream/fidelity-report-issue-88.md` (recovered from `verifier`'s scratch file after a second branch-drift incident, below).
- **Second incident (near-miss, no damage) — `verifier` subagent left local HEAD on `main`.** After S2-09's audit completed, a routine `git branch --show-current` check (the exact process fix instituted after the first incident) caught local HEAD on `main` again, before any commit or push was attempted. Root cause: contrary to earlier observation that `qa-engineer`/`verifier` reliably use isolated worktrees, this `verifier` run apparently touched the shared working directory's branch state directly at some point (it also self-reported being blocked by `branch-guard.sh` from writing its fidelity-report file while on `main`, which it correctly respected rather than routing around). Verified `local main` matched `origin/main` exactly (`b34f7e2`, no divergence, working tree clean) before switching back to the integration branch — zero risk materialized. This confirms the post-incident-1 process fix (branch check before every git operation) works: the same failure mode recurred, but was caught immediately with no consequence, instead of repeating the incident. **No further process change needed beyond continuing that discipline** — but worth noting that `qa-engineer`/`verifier` are not as reliably isolated from this shared directory as first assumed.
- 2026-09-22: S2-10 delivered as [llipe/dev-tasks#240](https://github.com/llipe/dev-tasks/pull/240) — cross-repo consumer story (RF-63), zero code changes in memo-cli itself. 18 files across three tool mirrors (Claude/Copilot/Kiro) in dev-tasks: skill docs, `developer`/`technical-writer` agent definitions, `product-engineer`/`planner` prompts. Live-validated against `memo_eval`; dev-tasks' own full quality gate suite passed (typecheck/lint/format/test 1688 tests/audit). **Third incident (no damage, self-healed by the other party)**: mid-task, discovered `dev-tasks`' local checkout is actively driven by an unrelated _concurrent session_ running its own story-by-story orchestration there (branch moved through several states — `main` → `issue/89-...` → `story/S-003-...` → back to `integration/shared-understanding-phase-3` → `story/S-004-...` — without this run's involvement), and one commit briefly landed on the wrong branch (`story/S-003-vocabulary-section`) before being extracted via a scoped `git reset --soft` targeting only the 18 files this story touched, finished in an isolated worktree. I independently traced the full git history afterward (not trusting the subagent's account at face value): confirmed the concurrent session's own work was properly squash-merged via its own PR #238 with zero trace of the stray commit — it self-healed completely independently, because PR merges happen via GitHub, not local branch state. No data lost, no scope leaked either direction. Deliberately did **not** force the dev-tasks checkout back to where it started, since that session has since moved forward to a new story — restoring the old branch now would disrupt it, not protect it. PR #240 was left open (draft) for human review in `llipe/dev-tasks`, never merged by planner (same "no agent merges to main" rule extended to a different repo's `main`).
- **Self-correction — memo-cli's own skill copy gap.** S2-10's delegation incorrectly instructed skipping the sync of memo-cli's own `.claude/skills/memo-cli-usage/` copy, on an unverified assumption it was a placeholder — verified afterward that it was a real, stale 942-line v1-era doc with zero Phase 2 content, a genuine gap against the story's own AC6. Fixed immediately with a dedicated follow-up: PR [#103](https://github.com/llipe/memo-cli/pull/103), synced from the dev-tasks PR content. `qa-engineer`'s gate caught a real, if trivial, issue (`format:check` FAIL from a missing trailing newline in the task-list file) — fixed directly on the PR branch (a workstream-bookkeeping fix, consistent with edits made directly elsewhere in this run) and re-verified green before merging. Content-identical to dev-tasks' copy (confirmed diff, ignoring each repo's own prettier config differences — quote style, table padding only).
- 2026-09-22: **S2-11 merged (PR #104) — Phase 2 exit gate, the last of the 11 stories.** Hit a real AC1 blocker: `eval:relevance` measured **82.1%**, far below the 96.4% floor. Root cause (correctly diagnosed by `developer`, not a ranking regression): `memo_eval` had accumulated 12 leftover points from five different stories' own required manual smoke tests (S2-04, dev-tasks ISSUE-89, S2-08, and S2-11's own smoke test) across the phase, with no cleanup step anywhere — `scripts/eval-relevance.ts` queries the collection unfiltered by Phase 1 design, so the debris diluted the candidate pool. `developer` correctly stopped when its `memo delete` attempt was blocked by the safety classifier rather than routing around it. **I got explicit human approval before touching anything**, then personally (not delegated) did a precise, read-only Qdrant scroll of every point in `memo_eval`, diffed against the 44 official fixture ids from `tests/fixtures/relevance/entries.json`, and found **12** non-fixture points — 3 more than the 9 originally reported (including one, `f1eab8f2...` in `kb`, that I caught myself by noticing `kb`'s count was 45 instead of the expected 44). Deleted all 12 via `memo delete --id <id> --yes --json`, verified the collection back to exactly 44 points with zero non-fixture ids remaining, and re-ran the eval myself: **96.4%** overall, byte-identical to the recorded floor (concept 100%, identifier 100%, cross-repo 83.3%, recency 100%). Handed back to `developer` to finish AC7/AC8 (PRD changelog, §13 ticks, 1.3.0 release notes) with the clean number. `qa-engineer`'s gate then caught a second real issue: the PR's own new changelog row broke `format:check`, making its own claim that "`pnpm run validate` passes" false on the commit making that claim — fixed directly (one-line prettier fix) and re-verified genuinely green. `verifier` Audit Mode: Fidelity High, two Major (non-blocking) drift items — D1 (AC3's checkbox slightly overstates `write.ts`'s coverage resolution; the global gate genuinely passes, the per-file number doesn't) and **D2 (actioned immediately, not queued)**: the `memo_eval` pollution mechanism itself has no tracked GitHub issue despite causing a real blocker this phase — opened [issue #105](https://github.com/llipe/memo-cli/issues/105) with three concrete remediation options (defensive eval-harness filtering, mandatory teardown convention, or a dedicated smoke-test bank) for `product-engineer`/`developer` to decide on before Phase 3. Merge itself hit a GitHub-side caching quirk (`gh pr merge` repeatedly reported "head branch is out of date" even though the branch's merge-base with integration exactly equaled integration's tip) — resolved by closing and reopening the PR, which forced GitHub to resync its stale `mergeable_state` cache; not a real conflict, verified via direct `git merge-base` and the GitHub API before concluding it was safe to retry.
- **Session-wide summary at Phase 2 completion:** all 11 stories merged across 13 PRs (11 story PRs + 1 drift-fix PR for issue #98 + 1 skill-sync follow-up PR for #103), plus one still-open cross-repo PR (dev-tasks#240) awaiting human review. Three real incidents this run, all caught and resolved with zero lasting damage: (1) an accidental direct push to `main` from a stale branch checkout, reverted with explicit user confirmation; (2) a near-miss recurrence of the same failure mode, caught before any commit by the process fix instituted after incident 1; (3) a concurrent, unrelated session's work in a second repository briefly intersecting with this run's own commit, fully self-healed via that session's own independent PR-merge process. Two genuine production-quality bugs were found and fixed via live manual validation rather than only unit tests: a missing Qdrant payload index (`pending_contradiction`, S2-07) and a calendar-validation gap in date-only flag parsing (`--since`/`--from`/`--to`, found on S2-06, fixed via issue #98). The eval-relevance floor (96.4%) held throughout, including through a real migration (S2-09) and the end-of-phase pollution incident, both independently re-verified live rather than trusted from developer self-reports.
